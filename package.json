{
  "name": "paris-lille",
  "version": "1.0.4",
  "description": "Site Paris-Lille",
  "scripts": {
    "build": "npm run generate-version && npm run optimize-images && tailwindcss -i ./src/input.css -o ./css/main.css",
    "generate-version": "node scripts/generate-version.js",
    "watch": "npm run generate-version && tailwindcss -i ./src/input.css -o ./css/main.css --watch",
    "minify": "npm run generate-version && npm run optimize-images && tailwindcss -i ./src/input.css -o ./css/main.css --minify",
    "optimize-images": "node scripts/optimize-images.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.17",
    "autoprefixer": "^10.4.22",
    "fs-extra": "^11.3.2",
    "glob": "^13.0.0",
    "postcss": "^8.5.6",
    "sharp": "^0.34.5"
  },
  "dependencies": {
    "tailwindcss": "^4.1.17"
  }
}